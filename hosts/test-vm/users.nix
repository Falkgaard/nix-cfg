# Just a list of attribute sets that will be used as arguments to `../../libraries/system.nix`'s `mkHostUsers` function.
# TODO: Explain argument requirements.
[
   { username = "falk";  }
   #{ username = "guest"; } # TODO: Re-include at some point to test.
]
